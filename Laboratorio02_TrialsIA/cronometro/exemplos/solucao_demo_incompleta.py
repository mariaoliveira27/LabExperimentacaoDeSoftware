"""Solução incompleta para fins de demonstração (não passa nos testes).
Usada para validar cenários de estouro de timebox e interrupção.
"""

import sys


def processar():
    for linha in sys.stdin:
        if not linha:
            break
        # Solução incompleta de propósito
        print("NAO NAO NAO NAO")


if __name__ == "__main__":
    processar()
